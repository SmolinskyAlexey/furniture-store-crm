window.CatalogTemplates = class {    static renderCatalog(products = [], allow_edit = 0) {        return products.map(product =>{			            let imagesHtml = '';            if (product.images && product.images.length > 0) {                imagesHtml = product.images.map(image => {                    // Используем preview если есть, иначе full                    const  imageUrl = image.preview || image.full;                    return imageUrl ? `<img class="img-catalog" src="${imageUrl}"/>` : '';                }).join('');            }						// console.log(product);			            return `			            <tr>                <td><strong>#${product.id}</strong></td>                <td>                    ${imagesHtml}					<div style="vertical-align: top; display: inline-block;">						<small>${product.category}</small><br/>						${product.name}					</div>                </td>                                <td>${product.price}&nbsp;uah</td>                <td>${product.stock}</td>				`+(allow_edit ? 					`<td class="actions-cell">												<div class="actions-wrapper">								<button class="btn btn-icon btn-secondary" onclick="window.showUniversalModal('catalog', ${product.id})" title="Редактировать">✏️</button>						</div>											</td>                ` : ``)+`            </tr>			            `        }).join("");    };							};